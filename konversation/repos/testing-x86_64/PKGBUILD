# $Id$
# Maintainer: James Rayner <iphitus@gmail.com>
# Contributor: Sergio Jovani Guzman <moret@paretux.org>

pkgname=konversation
pkgver=1.1.967278
pkgrel=1
pkgdesc="A user friendly IRC client for KDE"
arch=(i686 x86_64)
url="http://konversation.kde.org"
license="GPL"
depends=('kdelibs' 'libxss' 'kdepimlibs' 'qt')
makedepends=('automoc4' 'cmake' 'pkgconfig')
source=(ftp://ftp.archlinux.org/other/$pkgname/$pkgname-$pkgver.tar.bz2
        #http://download2.berlios.de/$pkgname/$pkgname-$pkgver.tar.bz2
        )

build() {
  cd $srcdir/$pkgname
  cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release
  make || return 1
  make DESTDIR=$startdir/pkg install
}
md5sums=('93de4e7e282adcff30e35c12af3dfe96')
