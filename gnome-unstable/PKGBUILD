# $Id$
# Jan de Groot <jgc@archlinux.org>

pkgname=eog
pkgver=2.27.91
pkgrel=1
pkgdesc="Eye of Gnome: An image viewing and cataloging program"
arch=(i686 x86_64)
license=('GPL')
depends=('gnome-desktop>=2.27.5' 'libexif>=0.6.17' 'lcms>=1.18-2' 'desktop-file-utils' 'gnome-icon-theme>=2.27.90' 'exempi>=2.1.0')
makedepends=('pkgconfig' 'gnome-doc-utils>=0.17.4' 'intltool')
install=eog.install
groups=('gnome-extra')
options=(!emptydirs)
url="http://www.gnome.org"
source=(http://ftp.gnome.org/pub/gnome/sources/${pkgname}/2.27/${pkgname}-${pkgver}.tar.bz2)
sha256sums=('a3a4b9f3a3a35eb66c74bd000190b8a2e5454c2777e5ac3682ac3ae56d8fba93')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/usr --sysconfdir=/etc \
      --localstatedir=/var --disable-scrollkeeper || return 1
  make || return 1
  make GCONF_DISABLE_MAKEFILE_SCHEMA_INSTALL=1 DESTDIR="${pkgdir}" install || return 1
  
  install -m755 -d "${pkgdir}/usr/share/gconf/schemas"
  gconf-merge-schema "${pkgdir}/usr/share/gconf/schemas/${pkgname}.schemas" --domain eog ${pkgdir}/etc/gconf/schemas/*.schemas || return 1
  rm -f ${pkgdir}/etc/gconf/schemas/*.schemas
}
