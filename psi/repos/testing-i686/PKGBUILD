# $Id$
# Maintainer: Pierre Schmitz <pierre@archlinux.de>

pkgname=psi
pkgver=0.12.1
pkgrel=2
pkgdesc="Psi - Jabber client"
url="http://psi-im.org/"
license=('GPL')
arch=('i686' 'x86_64')
depends=('qca-ossl' 'aspell' 'libxss' 'xdg-utils')
install='psi.install'
source=("http://downloads.sourceforge.net/psi/psi-${pkgver}.tar.bz2")
md5sums=('da273e45daef04ffc8338708e96e9173')

build() {
	cd $startdir/src/psi-${pkgver}
	./configure --prefix=/usr --disable-bundled-qca || return 1
	make || return 1
	make INSTALL_ROOT=$startdir/pkg install
}
