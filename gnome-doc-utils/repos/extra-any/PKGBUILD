# $Id$
# Maintainer: Jan de Groot <jgc@archlinux.org>

pkgname=gnome-doc-utils
pkgver=0.18.1
pkgrel=1
pkgdesc="Documentation utilities for Gnome"
arch=('any')
license=('GPL' 'LGPL')
depends=('libxslt>=1.1.26' 'python>=2.6' 'docbook-xml' 'rarian>=0.8.1')
makedepends=('pkgconfig' 'intltool')
url="http://www.gnome.org"
source=(http://ftp.gnome.org/pub/gnome/sources/${pkgname}/0.18/${pkgname}-${pkgver}.tar.bz2)
sha256sums=('f601f602a99023fb8d46b3e5dec460357d719d9c6aae9600520f1b786d4f545f')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/usr --sysconfdir=/etc --mandir=/usr/share/man \
      --localstatedir=/var --disable-scrollkeeper || return 1
  make || return 1
  make DESTDIR="${pkgdir}" install || return 1
}
