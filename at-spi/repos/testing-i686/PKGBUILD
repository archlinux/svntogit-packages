# $Id$
# Maintainer: Jan de Groot <jan@archlinux.org>

pkgname=at-spi
pkgver=1.24.0
pkgrel=1
pkgdesc="Assistive Technology Service Provider Interface"
arch=(i686 x86_64)
license=('GPL')
depends=('libbonobo>=2.24.0' 'libxevie' 'libxtst' 'gtk2>=2.14.3')
makedepends=('perlxml' 'pkgconfig' 'python>=2.5')
options=('!libtool')
url="http://www.gnome.org"
source=(http://ftp.gnome.org/pub/gnome/sources/${pkgname}/1.24/${pkgname}-${pkgver}.tar.bz2)
md5sums=('e546a35e21d127b502a3c57550a189a0')

build() {
  cd ${startdir}/src/${pkgname}-${pkgver}
  ./configure --prefix=/usr --sysconfdir=/etc \
              --localstatedir=/var --disable-static \
	      --libexecdir=/usr/lib/at-spi || return 1
  make || return 1
  make DESTDIR=${startdir}/pkg install || return 1
}
