# $Id$
# Maintainer :  Ionut Biru <ibiru@archlinux.org>
# Contributor: Gour <Gour <gour@mail.inet.hr>

pkgname=libisoburn
pkgver=0.6.2.pl00
pkgrel=1
pkgdesc="frontend for libraries libburn and libisofs"
url="http://libburnia.pykix.org/wiki/Libisoburn"
arch=('i686' 'x86_64')
license=('GPL2')
depends=('libburn>=0.8.6' 'libisofs>=0.6.36' 'readline>=6.0')
options=('!libtool')
source=(http://files.libburnia-project.org/releases/${pkgname}-${pkgver}.tar.gz)
md5sums=('5066c9b9ac6ec598eb303714df0ec652')

build() {
  cd ${srcdir}/${pkgname}-${pkgver/.pl??/}
  ./configure --prefix=/usr
  make
  make DESTDIR=${pkgdir} install
}
