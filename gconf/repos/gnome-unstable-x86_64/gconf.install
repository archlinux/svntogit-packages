post_install() {
  ldconfig -r .
  chmod 755 etc/gconf/gconf.xml.system
}

post_upgrade() {
  ldconfig -r .
  chmod 755 etc/gconf/gconf.xml.system

  PID=`pidof gconfd-2`
  if [ ! -z "${PID}" ]; then
    kill ${PID}
  fi
}
