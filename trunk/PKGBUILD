# $Id$
# Maintainer: 
pkgname=meld
pkgver=1.2
pkgrel=1
pkgdesc="Visual diff and merge tool for GNOME"
arch=(i686 x86_64)
license=('GPL')
url="http://meld.sourceforge.net/"
depends=('gnome-python-desktop>=2.20.0-3 gtksourceview')
makedepends=('perlxml' 'pkgconfig' 'gnome-doc-utils>=0.11.2' 'intltool')
install=meld.install
source=(http://ftp.gnome.org/pub/gnome/sources/${pkgname}/1.2/${pkgname}-${pkgver}.tar.gz
	meld_buildfix.diff)
md5sums=('50ea5a7cdf2451448a78e53b172d7f6f'
         '074042dcafc9869a4dad69e0d1d92b0e')

build() {
  cd ${startdir}/src/${pkgname}-${pkgver}
  patch -Np1 -i ../meld_buildfix.diff || return 1
  make prefix=/usr || return 1
  make prefix=/usr DESTDIR=${pkgdir} install || return 1
  rm -rf ${startdir}/pkg/usr/gnome/share/doc
}
