# $Id$
# Maintainer: Dieter Plaetinck <dieter@plaetinck.be>

pkgname=aif
pkgver=2009.04.08
pkgrel=1
pkgdesc="The ArchLinux Installation Framework"
arch=('i686' 'x86_64')
url="http://projects.archlinux.org/?p=aif.git;a=summary"
license=('GPL3')
depends=('bash' 'pacman')
source=(http://ftp.archlinux.org/other/aif/aif-$pkgver.tar.gz)
md5sums=('131ae0e32743eb80708074ebd7104b30')

build() {

  cd $srcdir/aif-$pkgver/
  
  install -d $pkgdir/sbin
  install -d $pkgdir/usr/share/aif/docs
  install -d $pkgdir/usr/share/aif/examples
  install -d $pkgdir/usr/lib/aif/core
  install -d $pkgdir/usr/lib/aif/user
  install -D -m755 src/aif.sh $pkgdir/sbin/aif
  install -D -m644 HOWTO $pkgdir/usr/share/aif/docs
  install -D -m644 README $pkgdir/usr/share/aif/docs
  cp -rp src/core $pkgdir/usr/lib/aif
  chmod -R 755 $pkgdir/usr/lib/aif/core
  cp -rp src/user $pkgdir/usr/lib/aif  
  chmod -R 755 $pkgdir/usr/lib/aif/user
  cp -rp examples $pkgdir/usr/share/aif
  chmod -R 755 $pkgdir/usr/share/aif/examples

}

# vim:set ts=2 sw=2 et:

