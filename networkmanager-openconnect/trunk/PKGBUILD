# $Id$
# Maintainer: Ionut Biru <ionut@archlinux.ro>

pkgname=networkmanager-openconnect
pkgver=0.7.997
pkgrel=1
pkgdesc="NetworkManager VPN integration for openconnect"
arch=('i686' 'x86_64')
license=('GPL')
url="http://www.gnome.org/projects/NetworkManager/"
depends=('networkmanager>=0.7.998' 'openconnect')
makedepends=('intltool' 'gtk2' 'libglade' 'gconf' 'gnome-keyring')
optdepends=('network-manager-applet: GNOME frontends to NetWorkmanager')
options=('!libtool')
source=(ftp://ftp.archlinux.org/other/NetworkManager-openconnect/NetworkManager-openconnect-${pkgver}.tar.bz2)
#source=(http://ftp.gnome.org/pub/GNOME/sources/NetworkManager-openconnect/0.7/NetworkManager-openconnect-${pkgver}.tar.bz2)
sha256sums=('ce06a4cf25fb57b7a99c63267e4f69e5bcbf8c3199cd191dc9b8842042597efd')

build() {
  cd "${srcdir}/NetworkManager-openconnect-${pkgver}"
  ./configure --prefix=/usr \
  	--sysconfdir=/etc \
	--libexecdir=/usr/lib/networkmanager \
	--disable-static || return 1
  make || return 1
  make DESTDIR="${pkgdir}" install || return 1
}
