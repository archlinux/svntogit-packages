# $Id$
# Maintainer: Eric Belanger <eric@archlinux.org>
# Contributor: Tom Newsom <Jeepster@gmx.co.uk>

pkgname=di
pkgver=4.18
pkgrel=1
pkgdesc="A disk information utility, displaying everything (and more) that your df command does"
arch=('i686' 'x86_64')
url="http://www.gentoo.com/di/"
license=('ZLIB')
depends=('glibc')
options=('!makeflags')
source=(http://www.gentoo.com/di/${pkgname}-${pkgver}.tar.gz)
md5sums=('caa2b12aacd33275c79cf8bcec41ca6d')
sha1sums=('11a6328c56b0fb9a2eee9eef4c3052509242383f')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  make || return 1
  make prefix="${pkgdir}/usr" install || return 1
  ln -sf di "${pkgdir}/usr/bin/mi"
  install -D -m644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
}
