# $Id$
# Maintainer: Jan de Groot <jgc@archlinux.org>

pkgname=vte
pkgver=0.17.2
pkgrel=1
pkgdesc="VTE - Virtual Terminal Emulator"
arch=(i686 x86_64)
license=('LGPL')
depends=('gtk2>=2.13.7' 'libsm')
options=('!libtool')
makedepends=('pygtk>=2.12.1' 'pkgconfig' 'intltool')
url="http://www.gnome.org"
source=(http://ftp.gnome.org/pub/GNOME/sources/vte/0.17/vte-${pkgver}.tar.bz2
	vte-0.16.0-reaper-python-bindings.patch)
md5sums=('515cd053e2c25423793ea4e057e3b018' 'efe29cd40635e73fa3152f77d91a4f39')

build() {
  cd ${startdir}/src/${pkgname}-${pkgver}
  patch -Np1 -i ${startdir}/src/vte-0.16.0-reaper-python-bindings.patch || return 1
  ./configure --prefix=/usr --sysconfdir=/etc \
              --libexecdir=/usr/lib/vte \
              --localstatedir=/var --disable-static || return 1
  make || return 1
  make DESTDIR=${startdir}/pkg install || return 1
}
