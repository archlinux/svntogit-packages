service sane-port
{
	port        = 6566
	socket_type = stream
	wait        = no
	user        = nobody 
	group       = scanner
	server      = /usr/sbin/saned
	# disabled by default!
	disable     = yes
		  }
