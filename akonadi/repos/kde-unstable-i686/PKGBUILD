# $ Id: $
# Maintainer: Pierre Schmitz <pierre@archlinux.de>

pkgname=akonadi
pkgver=1.0.80
pkgrel=1
pkgdesc="PIM layer, which provides an asynchronous API to access all kind of PIM data"
arch=('i686' 'x86_64')
url='http://www.akonadi-project.org'
license=('LGPL')
depends=('qt>=4.4' 'shared-mime-info' 'mysql')
makedepends=('pkgconfig' 'cmake' 'automoc4' 'libxslt')
install='akonadi.install'
source=("http://akonadi.omat.nl/${pkgname}-${pkgver}.tar.bz2")
md5sums=('832f65cc22a8540f048534a9b4d490a8')

build() {
	cd $srcdir
	mkdir build
	cd build
	cmake ../${pkgname}-${pkgver} \
		-DCMAKE_BUILD_TYPE=Release \
		-DCMAKE_INSTALL_PREFIX=/usr
	make
	make DESTDIR=$pkgdir install
}
