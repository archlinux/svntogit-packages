# $Id$
# Maintainer: Andrea Scarpino <andrea@archlinux.org>

pkgname=phonon-vlc
pkgver=0.2.0
pkgrel=2
arch=('i686' 'x86_64')
url="http://phonon.kde.org"
pkgdesc="Phonon VLC backend"
license=('LGPL')
depends=('vlc')
makedepends=('cmake' 'automoc4' 'phonon')
provides=('phonon-backend')
source=("http://download.kde.org/stable/phonon-backend-vlc/${pkgver}/src/phonon-backend-vlc-${pkgver}.tar.gz")
md5sums=('33a1ca367989e306507ea633c99b8df5')

build() {
  cd ${srcdir}
  mkdir build
  cd build
  cmake ../phonon-backend-vlc-${pkgver} \
    -DCMAKE_BUILD_TYPE=Release \
    -DCMAKE_INSTALL_PREFIX=/usr
  make
}

package() {
  cd ${srcdir}/build
  make DESTDIR=${pkgdir} install
}
