service swat
{
        socket_type             = stream
        wait                    = no
        user                    = root
        server                  = /usr/bin/swat
        log_on_success  += HOST DURATION
        log_on_failure  += HOST
        disable                 = yes
}
