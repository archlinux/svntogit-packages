# $Id$
# Maintainer: Jan de Groot <jgc@archlinux.org>

pkgname=poppler
pkgver=0.10.6
pkgrel=1
pkgdesc="PDF rendering library based on xpdf 3.0"
arch=(i686 x86_64)
license=('GPL')
depends=('libjpeg' 'gcc-libs>=4.3.3' 'cairo>=1.8.6' 'libxml2>=2.7.3' 'fontconfig>=2.6.0' 'openjpeg>=1.3-3')
makedepends=('pkgconfig')
optdepends=('poppler-data')
options=('!libtool')
url="http://poppler.freedesktop.org/"
source=(http://poppler.freedesktop.org/${pkgname}-${pkgver}.tar.gz)
md5sums=('e1a2d98ba5a4199f20d7637a7d79a829')

build() {
  cd "${srcdir}/${pkgname}-${pkgver}"
  ./configure --prefix=/usr --sysconfdir=/etc \
              --localstatedir=/var --disable-static \
	      --enable-cairo-output \
	      --enable-xpdf-headers \
	      --enable-libjpeg --enable-zlib \
	      --disable-poppler-qt4 \
	      --disable-poppler-glib \
	      --disable-poppler-qt \
	      --disable-gtk-test || return 1
  make || return 1
  make DESTDIR="${pkgdir}" install || return 1
}
